# `E027.md` — per-case evidence

- **case_id:** `E027`
- **bucket:** `EDGE`
- **scenario:** legal bounded `MAX_HITS` subheader run on one active lane
- **contract anchor:** subheader `sub_hit_cnt=MAX_HITS` from the upstream [`feb_frame_assembly/`](../../../external/mu3e-ip-cores/feb_frame_assembly/) contract, promoted only in a configuration where total event hits stay within the default `OPQ_N_HIT=2047` budget
- **stage taps:** `I · O · D`
- **status:** `✅`

## Execution method

| method | harness | make_target | plusargs | seed |
|:---:|---|---|---|---|
| D | `uvm` | `make ip-uvm-basic` | `+SWB_PROFILE_NAME=E027_max_hit_lane0_only +SWB_CASE_SEED=113 +SWB_FRAMES=1 +SWB_HIT_MODE=max +SWB_FEB_ENABLE_MASK=1 +SWB_SAT0=0.20 +SWB_SAT1=0.20 +SWB_SAT2=0.20 +SWB_SAT3=0.20` | `113` |

## Execution evidence

| outcome | sim_time | txn_count | ingress_hits | opq_hits | dma_hits | payload_words | padding_words | ghost_missing | log_path | ucdb_path |
|:---:|---|---:|---:|---:|---:|---:|---:|---|---|---|
| ✅ | `20.550 us` | 1 | 4096 | 1024 | 1024 | 256 | 128 | `ghost=0, missing=0` | [`../../cases/basic/uvm/report/E027_max_hit_lane0_only_summary.txt`](../../cases/basic/uvm/report/E027_max_hit_lane0_only_summary.txt) | pending |

## Notes

- The raw ingress monitor still sees all generated FEB traffic before lane masking, so `ingress_hits=4096` is expected. The promoted legal contract is the masked OPQ-visible subset, which closes at `1024` hits.
- The earlier all-lane `MAX_HITS` attempt is not promoted as a pass criterion because it exceeds the default `OPQ_N_HIT` capacity and therefore tests a configuration-limit condition, not the legal integrated contract.
